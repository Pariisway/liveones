// Firebase Auth Functions
console.log("🔐 Loading auth functions...");

function showLogin() {
    alert("Login modal would open here. Connect with Firebase!");
}

function showSignup() {
    alert("Signup modal would open here. Connect with Firebase!");
}

function logout() {
    alert("Logout functionality. Connect with Firebase!");
}

// Initialize auth
document.addEventListener('DOMContentLoaded', function() {
    console.log("Auth functions loaded");
});
